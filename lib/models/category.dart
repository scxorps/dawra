class Category {
  final String id;
  final String title;
  final String imageUrl; // Corrected camelCase naming convention for imageUrl

  // Constructor with named parameters
const Category({
    required this.id,
    required this.title,
    required this.imageUrl,
  });
}
